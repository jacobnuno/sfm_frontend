import namespace from '@/utils/namespace';

export default namespace('day', {
    getters: [

    ],
    actions: [
        'createDay',
        'getDays',
        'getDay',
        'updateDay',
        'deleteDay'
    ],
    mutations: [

    ]
});
